{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} {{ page_name }}{% endblock %}

{% block body %}
    <div class="jumbotron mt-2">
        <h1 class="text-center display-4 lead">{{ page_name }}</h1>
        {{ form_start(form) }}

        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary btn-lg btn-block w-100">Modifier</button>

        {{ form_end(form) }}
    </div>
    <a href="{{ path('lieu_affichage') }}"><button class="btn btn-lg btn-dark btn-block w-100">Annuler</button></a>

{% endblock %}