
{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} {{ page_name }}{% endblock %}

{% block body %}
    <div class="jumbotron mt-2">
        <h1 class="text-center display-4 lead">{{ page_name }}</h1>
        {{ form_start(form) }}

        {{ form_errors(form) }}
        {{ form_widget(form) }}



        <a href="{{ path('main_home') }}"><input type="button" value="Annuler"/></a>
        <button type="submit">Ajouter ville</button>

        <i class ='btn btn-primary w-1950'><a href="{{ path('sortie_add') }}"><input type="button"  value="Retour sur créé une sortie"></a></i>

        {{ form_end(form) }}

    </div>
{% endblock %}