
{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} {{ page_name }}{% endblock %}

{% block body %}
    <div class="jumbotron mt-2">
        <h1 class="text-center display-4 lead">{{ page_name }}</h1>
        {{ form_start(form) }}

        {{ form_errors(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary btn-lg btn-block w-100">Ajouter ville</button>
        <a href="{{ path('list_villes') }}"><input type="button" class="btn btn-dark btn-lg btn-block w-100" value="Annuler"/></a>

        {{ form_end(form) }}

    </div>
{% endblock %}